name: DevOps AI Pipeline

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      AWS_REGION: us-east-1
      AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}
      ECR_REPO: devops-ai-demo
      IMAGE_TAG: ${{ github.sha }}
      INSTANCE_NAME: github-devops-instance
      KEY_NAME: MyKeyPair
      ROLE_NAME: EC2ECRAccessRole
      INSTANCE_PROFILE_NAME: EC2ECRAccessRole

    steps:
      # 1️⃣ Checkout del repositorio
      - name: Checkout
        uses: actions/checkout@v4

      # 2️⃣ Configura credenciales AWS
      - name: Configure AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      # 3️⃣ Login en ECR
      - name: Login to ECR
        id: ecr
        uses: aws-actions/amazon-ecr-login@v2

      # 4️⃣ Construir y subir imagen a ECR
      - name: Build & Push Docker Image
        env:
          IMAGE_URI: ${{ steps.ecr.outputs.registry }}/${{ env.ECR_REPO }}:${{ env.IMAGE_TAG }}
        run: |
          echo "🐳 Building and pushing image..."
          docker build -t ${{ env.ECR_REPO }} .
          docker tag ${{ env.ECR_REPO }}:latest ${{ env.IMAGE_URI }}
          docker push ${{ env.IMAGE_URI }}
          echo "IMAGE_URI=${{ env.IMAGE_URI }}" >> $GITHUB_ENV

      # 5️⃣ Revisión con IA (opcional)
      - name: AI Review
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          if [ -z "$OPENAI_API_KEY" ]; then
            echo "⚠️  No OpenAI API key set. Skipping AI analysis."
            exit 0
          fi
          echo "🤖 Running AI Review..."
          curl -s https://api.openai.com/v1/chat/completions \
            -H "Authorization: Bearer $OPENAI_API_KEY" \
            -H "Content-Type: application/json" \
            -d '{"model":"gpt-4o-mini","messages":[{"role":"user","content":"Review this Dockerfile:\n\n'"$(cat Dockerfile)"'"}]}' \
            | jq -r '.choices[0].message.content'

      # 6️⃣ Crear recursos AWS automáticamente (Key Pair + IAM)
      - name: Ensure Key Pair and IAM Role
        run: |
          REGION="${{ env.AWS_REGION }}"
          KEY_NAME="${{ env.KEY_NAME }}"
          ROLE_NAME="${{ env.ROLE_NAME }}"
          INSTANCE_PROFILE_NAME="${{ env.INSTANCE_PROFILE_NAME }}"

          # Crear Key Pair si no existe
          if ! aws ec2 describe-key-pairs --key-names "$KEY_NAME" --region "$REGION" >/dev/null 2>&1; then
            echo "🔑 Creating Key Pair..."
            aws ec2 create-key-pair --key-name "$KEY_NAME" --query 'KeyMaterial' --output text > "$KEY_NAME.pem"
            chmod 400 "$KEY_NAME.pem"
          fi

          # Crear IAM Role si no existe
          if ! aws iam get-role --role-name "$ROLE_NAME" >/dev/null 2>&1; then
            echo "🛡️ Creating IAM Role..."
            aws iam create-role \
              --role-name "$ROLE_NAME" \
              --assume-role-policy-document '{
                "Version": "2012-10-17",
                "Statement": [
                  {
                    "Effect": "Allow",
                    "Principal": { "Service": "ec2.amazonaws.com" },
                    "Action": "sts:AssumeRole"
                  }
                ]
              }'
            aws iam attach-role-policy \
              --role-name "$ROLE_NAME" \
              --policy-arn arn:aws:iam::aws:policy/AmazonEC2ContainerRegistryReadOnly
          fi

          # Crear Instance Profile si no existe
          if ! aws iam get-instance-profile --instance-profile-name "$INSTANCE_PROFILE_NAME" >/dev/null 2>&1; then
            echo "🛠️ Creating Instance Profile..."
            aws iam create-instance-profile --instance-profile-name "$INSTANCE_PROFILE_NAME"
          fi

          # Asociar Role al Instance Profile si no está asociado
          ROLES_IN_PROFILE=$(aws iam get-instance-profile --instance-profile-name "$INSTANCE_PROFILE_NAME" \
            --query 'InstanceProfile.Roles[*].RoleName' --output text)
          if [[ "$ROLES_IN_PROFILE" != *"$ROLE_NAME"* ]]; then
            echo "🔗 Associating Role to Instance Profile..."
            aws iam add-role-to-instance-profile \
              --instance-profile-name "$INSTANCE_PROFILE_NAME" \
              --role-name "$ROLE_NAME"
          fi

      # 7️⃣ Lanzar la instancia EC2
      - name: Launch EC2 Instance
        run: |
          echo "🚀 Launching EC2 instance..."

          AMI=$(aws ec2 describe-images --owners amazon \
            --filters "Name=name,Values=amzn2-ami-hvm-*-x86_64-gp2" "Name=state,Values=available" \
            --query 'Images | sort_by(@, &CreationDate) | [-1].ImageId' \
            --output text)

          SUBNET_ID=$(aws ec2 describe-subnets \
            --query 'Subnets[0].SubnetId' --output text --region ${{ env.AWS_REGION }})

          SECURITY_GROUP_ID=$(aws ec2 describe-security-groups \
            --query 'SecurityGroups[0].GroupId' --output text --region ${{ env.AWS_REGION }})

          echo "📦 Using Subnet: $SUBNET_ID"
          echo "🛡️  Using Security Group: $SECURITY_GROUP_ID"

          printf '%s\n' \
            '#!/bin/bash' \
            'yum update -y' \
            'yum install -y docker awscli' \
            'systemctl start docker' \
            'systemctl enable docker' \
            'usermod -aG docker ec2-user' \
            'aws ecr get-login-password --region '"${{ env.AWS_REGION }}"' | docker login --username AWS --password-stdin '"${{ env.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com"' ' \
            'docker pull '"${{ env.IMAGE_URI }}"'' \
            'docker run -d -p 80:80 '"${{ env.IMAGE_URI }}"'' > user-data.sh

          INSTANCE_ID=$(aws ec2 run-instances \
            --image-id "$AMI" \
            --instance-type t3.micro \
            --key-name ${{ env.KEY_NAME }} \
            --security-group-ids "$SECURITY_GROUP_ID" \
            --subnet-id "$SUBNET_ID" \
            --iam-instance-profile Name=${{ env.INSTANCE_PROFILE_NAME }} \
            --tag-specifications "ResourceType=instance,Tags=[{Key=Name,Value=${{ env.INSTANCE_NAME }}}]" \
            --user-data file://user-data.sh \
            --query 'Instances[0].InstanceId' --output text)

          echo "✅ Instance created with ID: $INSTANCE_ID"
          echo "INSTANCE_ID=$INSTANCE_ID" >> $GITHUB_ENV

      # 8️⃣ Obtener IP pública
      - name: Get Public IP
        run: |
          echo "⏳ Waiting for EC2 to be ready..."
          aws ec2 wait instance-running --instance-ids ${{ env.INSTANCE_ID }}
          PUBLIC_IP=$(aws ec2 describe-instances \
            --instance-ids ${{ env.INSTANCE_ID }} \
            --query 'Reservations[].Instances[].PublicIpAddress' --output text)
          echo "🌍 EC2 Public IP: $PUBLIC_IP"
          echo "PUBLIC_IP=$PUBLIC_IP" >> $GITHUB_ENV

      # 9️⃣ Confirmación final
      - name: Done
        run: |
          echo "🎉 Deployment completed successfully!"
          echo "Access your app at: http://${{ env.PUBLIC_IP }}"
